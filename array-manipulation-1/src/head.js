/* exported head */
/* receive array argument
place first element of the array into a separate variable
return that variable */

function head(array) {
  var firstElement = array[0];
  return firstElement;
}
