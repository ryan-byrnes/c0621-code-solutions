/* exported last */
/* receive array argument
remove last element and place in a variable
return variable */

function last(array) {
  var lastElement = array[array.length - 1];
  return lastElement;
}
